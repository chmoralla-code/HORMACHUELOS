// Disable PostCSS — AI-Forge uses plain CSS, no Tailwind.
// This empty config prevents Vite from walking up the directory tree
// and inheriting the parent Documents/postcss.config.js (tailwind).
export default {};